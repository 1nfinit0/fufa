---

---

<div class="relative flex items-center justify-center">
  <span class="umbrella absolute -top-4 -left-4 text-6xl md:text-7xl lg:text-8xl z-0 -rotate-25"> ☂️ </span>
<!-- Glow controlado -->
<span
  class="absolute mx-auto py-4 flex w-fit
         bg-linear-to-r from-gree-800 via-yellow-800 to-red-800
         bg-clip-text text-transparent
         text-2xl md:text-3xl lg:text-4xl
         font-extrabold italic
         blur-lg opacity-90
         select-none z-0 text-center"
>
  Nueva Colección Verano 2026
</span>

<!-- Texto principal -->
<h1
  class="relative py-4 flex justify-center w-fit
         bg-linear-to-r from-amber-400  to-pink-600
         bg-clip-text text-transparent
         text-2xl md:text-3xl lg:text-4xl
         font-extrabold italic
         drop-shadow-[0_2px_6px_rgba(0,0,0,0.15)]
         [-webkit-text-stroke:0.5px_rgba(255,255,255,0.6)]
         z-10 text-center"
>
  Nueva Colección Verano 2026
</h1>


  <span class="sun absolute bottom-4 -right-7 text-6xl md:text-7xl lg:text-8xl z-0"> ☀️ </span>
</div>

<style>
.sun {
  animation: sunMoveWithSpin 10s ease-in-out infinite alternate;
}

@keyframes sunMoveWithSpin {
  0% {
    transform: translateY(0) rotate(0deg);
  }
  50% {
    transform: translateY(-15px) rotate(180deg);
  }
  100% {
    transform: translateY(0) rotate(360deg);
  }
}
  .umbrella {
    animation: umbrellaMove 5s ease-in-out infinite alternate;
  }
  @keyframes umbrellaMove {
    0% {
      transform: translateY(0) rotate(-25deg);
    }
    100% {
      transform: translateY(30px) rotate(-25deg);
    }
  }
</style>